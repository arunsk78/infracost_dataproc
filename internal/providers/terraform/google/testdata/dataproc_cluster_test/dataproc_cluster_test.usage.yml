version: 0.1
resource_usage:
# If DataprocCluster has any defined usage parameters uncomment the lines below.
# Then add your configurations making sure to change the names of resource/usage params.
#
 google_dataproc_cluster.dataproc_cluster:
   monthly_data_processed_gb: 200
#
# Also add a usage example into the infracost-usage-example.yml with comments
# describing each parameter (be aware that its resources are ordered alphabetically):
#
# google_dataproc_cluster.my_dataproc_cluster:
#   my_usage_parameter: 100000000 # Monthly requests to the resource
